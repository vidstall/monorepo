---
phase: 01-foundation-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/core/constants.move
  - sources/core/token.move
  - sources/core/network_registry.move
  - sources/access/caps.move
  - sources/access/cp_queries.move
  - sources/miner/miner_store.move
  - sources/miner/staking.move
  - sources/miner/registration.move
  - tests/helpers.move
  - tests/core/network_registry_tests.move
  - tests/miner/registration_tests.move
  - tests/access/cp_queries_tests.move
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-07
  - FOUND-08
  - FOUND-09
  - FOUND-10
  - FOUND-11
  - FOUND-12

must_haves:
  truths:
    - "All 8 source modules comply with spec (docs/phase1-foundation.md and decentralized_video_conference-rev4.md)"
    - "All error codes match namespace table: 100s network_registry, 200s staking, 300s miner_store, 400s registration"
    - "All cap constructors are public(package) with no external minting paths"
    - "Paused-flag is checked on all state-mutating entry points (register, top_up_stake)"
    - "Basis-point invariants enforced (weights sum 10,000, ratios sum 10,000)"
    - "All 34 tests pass with no regressions after any fixes"
  artifacts:
    - path: "sources/core/constants.move"
      provides: "All numeric parameters"
    - path: "sources/core/token.move"
      provides: "DVCONF fungible coin + TreasuryCap"
    - path: "sources/core/network_registry.move"
      provides: "Singleton governance config + AdminCap"
    - path: "sources/access/caps.move"
      provides: "ControlPlaneCap, MinerCap with public(package) constructors"
    - path: "sources/access/cp_queries.move"
      provides: "CP-gated read queries into MinerStore"
    - path: "sources/miner/miner_store.move"
      provides: "Shared store of miner profiles + role sets"
    - path: "sources/miner/staking.move"
      provides: "StakePosition object (owned, per miner)"
    - path: "sources/miner/registration.move"
      provides: "Public entry points for miner lifecycle"
  key_links:
    - from: "sources/miner/registration.move"
      to: "sources/miner/staking.move"
      via: "stake operations during register/unregister"
      pattern: "staking::(new|destroy|lock|unlock)"
    - from: "sources/access/cp_queries.move"
      to: "sources/miner/miner_store.move"
      via: "package-private validator_set() accessor"
      pattern: "miner_store::validator_set"
    - from: "sources/miner/registration.move"
      to: "sources/core/network_registry.move"
      via: "reads min_stake and paused flag"
      pattern: "network_registry::(min_stake|is_paused)"
---

<objective>
QC Agent re-review of all 8 foundation modules against the Phase 1 spec and system architecture spec. Fix any spec-correctness findings. Confirm all 34 tests pass with no regressions.

Purpose: Validate that existing Phase 1 contracts are correct before deploying to testnet. The previous QC review found C1 + C2 issues which were fixed -- this re-review confirms those fixes held and checks for any remaining issues.

Output: All 8 modules verified compliant, QC APPROVED status, 34 tests green.
</objective>

<execution_context>
@C:/Users/alienware x17r2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alienware x17r2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/AGENT_ROUTING.md
@docs/skills/QC_AGENT_SKILL.md
@docs/skills/ONCHAIN_AGENT_SKILL.md
@docs/phase1-foundation.md
@docs/decentralized_video_conference-rev4.md
@docs/phase1/README.md
@docs/phase1/flows.md
@docs/phase1/constants.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: QC re-review of all 8 foundation modules</name>
  <files>
    sources/core/constants.move
    sources/core/token.move
    sources/core/network_registry.move
    sources/access/caps.move
    sources/access/cp_queries.move
    sources/miner/miner_store.move
    sources/miner/staking.move
    sources/miner/registration.move
    tests/helpers.move
    tests/core/network_registry_tests.move
    tests/miner/registration_tests.move
    tests/access/cp_queries_tests.move
  </files>
  <action>
    IMPORTANT: Before starting, read `.planning/AGENT_ROUTING.md` then read `docs/skills/QC_AGENT_SKILL.md` in full. This task IS the QC review.

    Perform the QC Agent OnChain review checklist against all 8 source modules:

    1. **Error Codes (FOUND-11):** Cross-reference every error constant in all modules against the namespace table in `docs/phase1/README.md`:
       - network_registry: 100 E_INVALID_WEIGHT, 101 E_INVALID_THRESHOLD, 102 E_INVALID_RATIO
       - staking: 200 E_INSUFFICIENT_STAKE, 201 E_STAKE_LOCKED, 202 E_NOT_OWNER
       - miner_store: 300 E_NOT_REGISTERED
       - registration: 400 E_INSUFFICIENT_STAKE, 401 E_STAKE_LOCKED, 402 E_NOT_OWNER, 403 E_PROTOCOL_PAUSED, 404 E_ALREADY_REGISTERED
       Flag any mismatch (wrong number, wrong name, missing constant, undocumented code).

    2. **Visibility & Access Control (FOUND-07, FOUND-09):**
       - Verify ALL cap constructors (`AdminCap`, `ControlPlaneCap`, `MinerCap`) use `public(package)` -- NOT `public`.
       - Verify `miner_store::validator_set()` is `public(package)`.
       - Verify `staking::lock()` and `staking::unlock()` are `public(package)`.
       - Flag any function that should be package-private but is public, or vice versa.

    3. **Arithmetic Invariants (FOUND-03):**
       - In `network_registry.move`, verify that scoring weight validation enforces sum == 10,000 basis points.
       - Verify reward ratio validation enforces sum == 10,000 basis points.
       - Verify threshold validation enforces descending order.
       - Confirm no floating-point math anywhere (all u64).

    4. **Paused-Flag Enforcement (FOUND-08):**
       - In `registration.move`, verify `register()` checks `!is_paused(registry)` and aborts E_PROTOCOL_PAUSED (403).
       - Verify `top_up_stake()` checks `!is_paused(registry)` and aborts E_PROTOCOL_PAUSED (403).
       - Check if any other state-mutating entry points exist that should also check the paused flag.

    5. **Stake Lock Enforcement (FOUND-05, FOUND-06):**
       - Verify `unregister()` aborts E_STAKE_LOCKED (401) if stake is locked.
       - Verify `unregister()` aborts E_NOT_OWNER (402) if caller is not owner.
       - Verify withdraw behavior when stake is unlocked.

    6. **Token & Registry (FOUND-01, FOUND-02):**
       - Verify `token.move` creates a proper fungible coin with TreasuryCap via `init`.
       - Verify `network_registry.move` creates a shared NetworkRegistry object via `init` with AdminCap transferred to publisher.

    7. **CP Queries (FOUND-10):**
       - Verify `cp_queries.move` provides external read access to validator set via ControlPlaneCap gating.

    8. **MinerStore (FOUND-09):**
       - Verify `miner_store.move` tracks validator set internally.
       - Verify external access is only through `cp_queries.move`.

    9. **Test Coverage (FOUND-04, FOUND-12):**
       - Verify test files cover all UC1-UC6 flows from `docs/phase1/flows.md`.
       - Check test assertions use named constants from `test_helpers` (no magic numbers).
       - Verify previous C1 + C2 fixes are still in place.

    10. **Previous QC findings:**
        - Confirm C1 and C2 fixes from the earlier QC review are still present and correct.

    Record all findings in a structured format:
    - **PASS**: Items that comply
    - **FAIL**: Items that violate spec correctness (must fix)
    - **NOTE**: Code quality observations (do not fix unless they violate spec correctness, per user decision)

    If any FAIL findings exist: fix them in the source files. If a fix changes behavior that a test depends on, update the test in the same change. Run `sui move test --silence-warnings` after every fix to confirm no regressions.

    If all items PASS: no source changes needed.
  </action>
  <verify>
    <automated>cd C:/Thesis/dvconf/dvconf-contracts && sui move test --silence-warnings</automated>
  </verify>
  <done>
    - QC review covers all 8 source modules and 3 test files
    - All error codes verified against namespace table (FOUND-11)
    - All cap constructors confirmed public(package) (FOUND-07)
    - Paused-flag enforcement confirmed on register and top_up_stake (FOUND-08)
    - Basis-point invariants confirmed (FOUND-03)
    - All 34 tests pass with no regressions (FOUND-12)
    - Any FAIL findings are fixed; any PASS/NOTE findings are documented
    - QC review output is recorded for the SUMMARY
  </done>
</task>

</tasks>

<verification>
All 34 tests pass: `sui move test --silence-warnings` shows 34 passing, 0 failing.
QC checklist fully executed against all 8 modules.
No spec-correctness violations remain.
</verification>

<success_criteria>
- QC Agent re-review complete for all 8 modules
- Error codes match namespace table exactly (FOUND-11)
- Cap visibility verified as public(package) (FOUND-07)
- Paused-flag enforcement verified (FOUND-08)
- Basis-point sums verified as 10,000 (FOUND-03)
- All 34 tests pass (FOUND-12)
- Zero FAIL findings remaining
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-validation/01-01-SUMMARY.md`
</output>
