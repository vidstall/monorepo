name = "oracle-devnet-sample"
env = "devnet"

[contract]
gas_budget = ""
create_registry_if_missing = false
force = false

# Reuse the DigitalOcean-hosted registry -- images are already published there
# (scenario/example/digitalocean.toml); pushing the same images to a second,
# separately-billed registry would be pure waste. Oracle Cloud instances just
# pull from it like any other host.
[registry]
provider = "digitalocean"
tag = ""

# All workers deliberately omit `region` and rely on OCI_REGION's default
# (secrets/cloud/oci.env) -- verified live (list_region_subscriptions) that
# this tenancy has exactly one subscribed region, ap-singapore-1 (its home
# region). Unlike azure.toml's multi-region example, spreading nodes across
# additional OCI regions here would just fail at apply time.

[[workers]]
host = "oracle-node-1"
service = "signaling"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-1"
service = "relay"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-1"
service = "cp-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-1"
service = "validator-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-2"
service = "signaling"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-2"
service = "relay"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-2"
service = "cp-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-2"
service = "validator-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-3"
service = "cp-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-3"
service = "cp-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 2

[[workers]]
host = "oracle-node-3"
service = "validator-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-4"
service = "validator-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 1

[[workers]]
host = "oracle-node-4"
service = "validator-daemon"
provider = "oci"
size = "VM.Standard.E2.1.Micro"
worker_index = 2
