# Inventory

This directory is reserved for inventory definitions or inventory-generation inputs.

The intended inventory shape is:

- `worker`
- `client`
- `stateful`

When Terraform outputs are connected, inventory generation should map provider-specific instances into those role groups.
