# Node Pool Module

This module is a provider-agnostic placeholder for role-based node pools.

It does not create infrastructure yet. It exists to define the shape of the shared interface that each provider root can consume later.
